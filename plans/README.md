# Implementation Plans

Generated by the improve skill on 2026-06-12, from an audit of the Windows
port slices on `main` (commits `987b51d4..f0b88e5c`, planned at `f0b88e5c`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make `pnpm package` work on a Windows box (electron-builder resources, backend path, ffmpeg fetch) | P1 | M | — | TODO |
| 002 | Fix the two Windows window-chrome bugs (preview window frame, theme-toggle base color) | P1 | S | — | DONE (2026-06-12; verify preview drag + theme toggle on the Windows box) |
| 003 | Pin the new platform seams (fifo.rs, capture_input.rs) with unit tests | P2 | S-M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- No hard dependencies between the three plans; 002 is the smallest and can
  go first or in parallel.
- All three should land BEFORE the Windows box arrives: 001 unblocks the
  Phase 1 on-box gate (`pnpm package:desktop:windows`), 002 removes two
  known-wrong Windows behaviors, 003 pins the seams that Phase 2
  (`docs/windows-port-plan.md`) will extend.
- 001 and 002 touch different files and cannot conflict. 003 touches only
  test code.

## Findings considered and rejected

- **Win11 version-floor parsing edge cases** (`enforceWindowsVersionFloor`,
  apps/desktop/src/main/index.ts ~2455): a malformed `os.release()` makes the
  parse NaN, which FAIL-CLOSES (dialog + quit) — the behavior is already the
  safe one. Not worth doing.
- **Trivial `source_zerocopy_enabled` stubs untested** (preview_camera.rs /
  preview_screen.rs): one-line constant stubs, already exercised through their
  callers' tests on macOS. Not worth standalone work.
- **Three platform-gating styles** (cfg blocks vs `cfg!()` vs Unsupported
  stubs): consistency note only; pattern guidance belongs in the Phase 2
  work that next touches these files, not a refactor now.
- **`check:windows` hardcodes `/opt/homebrew/opt/llvm/bin`**: machine-specific
  but this is a solo-dev repo and the path is the Homebrew default; revisit
  only if another contributor appears.
- **Multi-size `icon.ico`**: current single-entry 256px PNG-ICO is valid on
  Win10+; small sizes are OS-downscaled. Cosmetic; noted in plan 001's
  maintenance notes.
- **electron-builder `win.extraResources` "does not inherit" claim** (from
  the audit subagent): rejected as stated — verified in app-builder-lib
  source that global and platform lists CONCATENATE, which is the *opposite*
  problem and is what plan 001 actually fixes.
