# Reviewed candidates

Place a candidate here only when its three detailed calibration reports are retained or linked and
the executable hash, commit, hardware class, and macOS version remain auditable. Candidate files
are evidence only and are never loaded by a performance gate.
