# Active budgets

No v1 budget data is active. Activation requires explicit threshold values derived from reviewed
calibration evidence in a profile matching `../active-budget.schema.json`. Do not copy observed
maxima directly into this directory as limits.
